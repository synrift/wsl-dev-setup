# WSL Dev Setup

一键初始化 WSL Ubuntu 开发环境。

## Install

```bash
curl -fsSL https://raw.githubusercontent.com/synrift/wsl-dev-setup/main/install.sh | bash
```

```bash
GIT_NAME="xxx" \
GIT_EMAIL="xxx@xxx.com" \
INSTALL_DOCKER=1 \
bash -c "$(curl -fsSL https://raw.githubusercontent.com/synrift/wsl-dev-setup/main/install.sh)"
```
